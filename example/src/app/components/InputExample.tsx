import React, { type FC } from 'react';
import { Heading, View } from '@munyaal/mobile-ui';

const InputExample: FC = () => {
  return (
    <View flexDirection={'column'} gap={'$4'}>
      <Heading>Input Component</Heading>
    </View>
  );
};

export default InputExample;
